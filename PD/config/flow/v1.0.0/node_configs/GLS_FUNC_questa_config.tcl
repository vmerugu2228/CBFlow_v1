#!/usr/bin/env tclsh
# GLS_FUNC — Mentor Questa
array set gls_func {
    tool,vendor   "mentor"
    tool,name     "questa"
    tool,version  "v1.0.0"
    tool,args     "-64 -sv"
    sim,timescale "1ns/1ps"
}
