#!/usr/bin/env tclsh
# =============================================================================
# CALIBRE Tool Configuration - PV Flow
# =============================================================================
# Convention: calibre(common,var) shared, calibre(<node>,var) node-specific
# Override: set calibre(<key>) "value" in user_config.tcl
# =============================================================================

# --- COMMON (1 vars) ---
array set calibre {
    common,design_name                                   ""
}

